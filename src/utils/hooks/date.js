export function GetFormattedDate(dateString){
    const currentDate = newDAte(dateString);
    let monthcurrentDAte.getMonth();
    month = month+1;
    let day= currentDate.getDate();
    let year= currentDate.getFullYear();

    if(day<10) {
        day="0" +day;
    }
    if(month<10) {
        month
    }
}